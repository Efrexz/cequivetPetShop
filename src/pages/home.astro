---
import { Heart, Stethoscope, ShoppingBag } from "lucide-react";
---

<main>
  {/* CTA Section */}
  <section class="py-16 bg-muted">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl sm:text-4xl font-bold mb-6 text-foreground">
        Order Products via WhatsApp
      </h2>
      <p class="text-lg text-muted-foreground mb-8 max-w-2xl mx-auto">
        Browse our pharmacy and pet food sections, then easily order via
        WhatsApp. We offer convenient delivery or in-store pickup options.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/pharmacy"
          class="px-6 py-3 rounded-lg bg-primary text-white font-semibold text-lg hover:bg-primary/90 transition"
        >
          Shop Pharmacy
        </a>
        <a
          href="/food"
          class="px-6 py-3 rounded-lg border border-primary text-primary text-lg hover:bg-primary hover:text-white transition"
        >
          Shop Pet Food
        </a>
      </div>
    </div>
  </section>
</main>
